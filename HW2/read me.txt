利用ktmove1[k]和ktmove2[k]的兩個陣列來計算下一個跳躍位置
並計算跳躍位置能再次跳躍的出口數
取最少出口數的跳躍位置為下一位置

next_i[l]和next_j[k]為下一位置
npos為下一個跳躍位置的數目
exit[]陣列代表各跳躍位置的出口數

在npos=0的情況下，表示為無解。
在npos=1的情況下，表示必走此路徑。
在剩下情況下，計算最少出口數的跳躍位置。
最後印出m=1到m=8的陣列。